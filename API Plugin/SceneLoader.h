#pragma once
//#include "Utilities.h"
#include <iostream>

class SceneLoader
{
public:
	/*static void EnumScenes()
	{
		__int64 sceneList = Game::ConvertOffset(0x14F8308);
		std::cout << "Scene List: " << std::hex << sceneList << std::endl;

		for (int x = 0; x < 0x1D0; x++)
		{
			const char* sceneName = (const char*)(sceneList + x * 0x90 + 0x8);
			std::cout << "0x" << std::hex << std::uppercase << x << " : " << sceneName << std::endl;
		}
	}*/
};